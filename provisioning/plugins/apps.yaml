apiVersion: 1

apps:
  - type: 'required-grafanaprometheusmanager-app'
    org_id: 1
    org_name: 'required'
    disabled: false
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
